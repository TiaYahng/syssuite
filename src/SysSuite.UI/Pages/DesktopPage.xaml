<UserControl x:Class="SysSuite.UI.Pages.DesktopPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource App.Surface}" BorderBrush="{DynamicResource App.Border}" BorderThickness="1" CornerRadius="8" Padding="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel>
            <TextBlock Text="桌面整理" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource App.Text}" />
            <TextBlock Text="分区、规则与布局方案。" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{DynamicResource App.SubtleText}" />
            <TextBlock Text="MVP0 骨架" Margin="0,16,0,0" Foreground="{DynamicResource App.SubtleText}" />
        </StackPanel>
    </Border>
</UserControl>

<UserControl x:Class="SysSuite.UI.Pages.ToolboxPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource App.Surface}" BorderBrush="{DynamicResource App.Border}" BorderThickness="1" CornerRadius="8" Padding="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel>
            <TextBlock Text="工具箱" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource App.Text}" />
            <TextBlock Text="扩展工具入口。" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{DynamicResource App.SubtleText}" />
            <TextBlock Text="MVP0 骨架" Margin="0,16,0,0" Foreground="{DynamicResource App.SubtleText}" />
        </StackPanel>
    </Border>
</UserControl>

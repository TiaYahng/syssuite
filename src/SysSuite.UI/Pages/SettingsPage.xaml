<UserControl x:Class="SysSuite.UI.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource App.Surface}" BorderBrush="{StaticResource App.Border}" BorderThickness="1" CornerRadius="8" Padding="24" Width="720">
        <StackPanel>
            <TextBlock Text="??" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource App.Text}" />
            <TextBlock Text="???????????" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{StaticResource App.SubtleText}" />
            <Border Margin="0,20,0,0" Padding="16" Background="{StaticResource App.Background}" BorderBrush="{StaticResource App.Border}" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Watchdog ????" Foreground="{StaticResource App.Text}" />
                        <TextBlock Text="?????? HKCU Run?Watchdog ??????" Foreground="{StaticResource App.SubtleText}" TextWrapping="Wrap" />
                    </StackPanel>
                    <CheckBox x:Name="WatchdogAutoStartCheckBox" Grid.Column="1" Checked="OnWatchdogAutoStartChanged" Unchecked="OnWatchdogAutoStartChanged" />
                </Grid>
            </Border>
        </StackPanel>
    </Border>
</UserControl>

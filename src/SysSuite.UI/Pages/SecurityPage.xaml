<UserControl x:Class="SysSuite.UI.Pages.SecurityPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource App.Surface}" BorderBrush="{DynamicResource App.Border}" BorderThickness="1" CornerRadius="8" Padding="24" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel>
            <TextBlock Text="安全中心" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource App.Text}" />
            <TextBlock Text="防护状态、扫描与启动项。" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{DynamicResource App.SubtleText}" />
            <TextBlock Text="MVP0 骨架" Margin="0,16,0,0" Foreground="{DynamicResource App.SubtleText}" />
        </StackPanel>
    </Border>
</UserControl>
